# GitHub Container Registry Configuration
# Set this to your GitHub repository (owner/repo-name)
# Example: clayauld/meshtopo
GITHUB_REPOSITORY=clayauld/meshtopo

# SSL Configuration (for Traefik)
SSL_EMAIL=admin@example.com
SSL_DOMAIN=meshtopo.example.com

# DNS Challenge Configuration (optional)
# Uncomment and configure for DNS challenge instead of HTTP challenge
# DNS_PROVIDER=cloudflare
# DNS_EMAIL=admin@example.com
# DNS_API_TOKEN=your_cloudflare_api_token
# DNS_ZONE_API_TOKEN=your_cloudflare_zone_api_token

# Optional: Additional environment variables
# TZ=UTC

# Optional: Override image tags
# MESHTOPO_TAG=latest
